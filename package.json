{
  "name": "mcp-inspector",
  "version": "0.1.0",
  "description": "MCP protocol inspector CLI - visualize raw JSON-RPC messages",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "inspect": "node --import tsx/esm src/index.ts"
  },
  "dependencies": {
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
